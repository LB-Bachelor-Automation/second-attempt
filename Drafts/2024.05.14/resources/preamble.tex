% Import the inputenc package, which allows LaTeX to handle different character encodings
\usepackage[utf8]{inputenc}

% Import the fontenc package, which allows the use of special characters such as é and ö
\usepackage[T1]{fontenc}

% Import the graphicx package, which enables the insertion of images in the document
\usepackage{graphicx}

% Import the subfiles package, which allows the document to be split into smaller subfiles for easier management
\usepackage{subfiles}

% Import the cite package, which enables the use of citations in the document
\usepackage{cite}

% Import the soul package, which provides commands for highlighting and underlining text for improved readability
\usepackage{soul}

% Import the parskip package, which allows for easier paragraph spacing for improved readability
\usepackage{parskip}

% Import the glossaries package for creating a glossary
\usepackage[toc]{glossaries}
\renewcommand{\glossarypreamble}{\footnotesize}
\setlength{\glsdescwidth}{0.8\textwidth}
\setlength{\glspagelistwidth}{0.1\textwidth}

% highlighting quoting
\usepackage{csquotes}

% %to open svg files
% \usepackage{svg}